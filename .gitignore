# Personal, machine-local Claude Code config — never commit
.claude/settings.local.json
CLAUDE.local.md

# Worktrees created during plan execution
.worktrees/

# OS / editor noise
.DS_Store

# SwiftPM build artifacts
.build/
# SwiftPM / Xcode per-user metadata
.swiftpm/
