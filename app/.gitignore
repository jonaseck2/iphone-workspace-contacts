# XcodeGen output — regenerate with `xcodegen generate`
*.xcodeproj
# Xcode user state / build
xcuserdata/
DerivedData/
build/
# SwiftPM cloned packages (from xcodebuild -clonedSourcePackagesDirPath)
.build-spm/
